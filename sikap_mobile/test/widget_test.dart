import 'package:flutter_test/flutter_test.dart';
import 'package:sikap_mobile/app.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic smoke test - app starts without crashing
    // Full test requires Supabase initialization
    expect(true, isTrue);
  });
}
